general:
  build_dir: .

dependencies:
  cache_directories:
    - ".tox"
    - ".eggs"
  pre:
    - pip install tox tox-pyenv
    - pyenv local 2.7 3.6
    - sudo apt-get install libhunspell-dev

test:
  override:
    - tox
